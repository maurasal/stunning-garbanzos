{{!-- <div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}</h2>
  </div>
  <div class="row mt-4">
    {{#if jobs.length}}
    <h3>My Current List of Jobs</h3>
    <ul>
      {{#each jobs as |job|}}
      <li>
        <h3>{{job.jobTitle}}</h3>
        <p>Company Name:{{companyName}}</p>
        <p>Salary:{{salary}}</p>
        <p>Status:{{job.applicationStatus}}</p>
      </li>
    </ul>
    {{/each}}
  <p>You don't have any job applications yet.</p>
  {{/if}}
</div> --}}

<!-- Loop over each object in the projects array -->
{{!-- {{#each jobs as |job| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      <a href="/job/{{id}}">{{job_title}}</a>
    </h2>
    {{!-- <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p> --}}
  {{!-- </div>
  <div class="col-md-7">
    <p>
      {{application_status}}
    </p>
  </div>
</div>
{{!-- {{/each}} --}}